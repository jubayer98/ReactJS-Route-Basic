import Banner from "../components/Banner";

const ErrorPage = () => {
    return (
        <div>
            {/* Displaying the Banner component at the top of the error page */}
            <Banner />
        </div>
    );
};

export default ErrorPage;
